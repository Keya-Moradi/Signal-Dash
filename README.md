# Signal-Dash
Lightweight internal tool that takes experiments from **intake → approval → running → readout → decision**, tracks exposures/conversions, computes basic A/B stats, and generates an **AI-assisted readout** (with a non-AI fallback).
